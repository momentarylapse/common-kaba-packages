use opt.*


func main()
	var ff_test = func(p: f32[])  sum_sqr([for x in -5.0:5.0:0.2  p[0] * exp(-x^2 * p[1]) - 3 * exp(-x^2 * 0.7)])
	print(optimize([1.0,1.0], ff_test))
	# TODO replace with a "real"/stable test... but for now this is useful enough
