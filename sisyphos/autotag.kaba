use .file.*
use .config.*
use .tag.*
use os.*
use filetags.get_tags


func auto_tag(out f: LocalFile)
	try
		let tags = get_tags(f.path)
		for k=>v in tags
			f.tag_set([k, v])
	except Exception as e
		error_out(e)
