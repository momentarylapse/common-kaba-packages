// Description
Testet, ob die Maustaste gerade losgelassen wurde (ob sie im letzten Frame gedr&uckt war, es in diesem aber nicht mehr ist).
// Parameter button
in
Die zu testende Maustaste (0 = linke, 1 = mittlere, 2 = rechte).
// Return
<c>true</c>, falls die Maustaste gerade losgelassen wurde, ansonsten <c>false</c>.
#
