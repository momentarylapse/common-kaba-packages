use common
use subst
use simplify

shared Term zeros(shared Term t, string var)
	shared Term[] z
	for i in -1000:1000
		let r = subst_variable(t, var, AddNumber(i))
		r = normalize(r)
		if r.type == TYPE_NUMBER
			if r.name == "0"
				z.add(AddNumber(i))
	return AddList(z)
