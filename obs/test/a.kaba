use obs


class A as obs.Node
	var source = obs.Source(&self, &ind, "source")


class B as obs.Node
	var sink: obs.Sink
	func override __init__()
		sink.__init2__(&self, func() print("hi!"))


func main()
	var a: A
	var b: B
	obs.subscribe(a.source, b.sink)
	a.source.notify()

