use os
use kaba
use terminal



func main()
	print("welcome to kaba {{kaba_version}}")
	string[] history
	while true
		try
			let s = fancy_read_command(">>> ", history)
			if s != ""
				default_context.execute_single_command(s)
		except Exception as e
			print("exit")
			break
